import {Meta, Story, Preview} from '@storybook/addon-docs/blocks'
import {action} from '@storybook/addon-actions'
import {withKnobs, boolean} from '@storybook/addon-knobs'
import {PrimaryButton, SecondaryButton, TertiaryButton} from './Buttons'

<Meta title="Buttons" component={PrimaryButton} />

# Buttons

Buttons are used to trigger actions to interact with the application.

## Primary Buttons

<Preview withToolbar>
  <Story name="Primary">
    <PrimaryButton disabled={boolean("Disable Button", false)} onClick={action('button-click')}>Hello World</PrimaryButton>
  </Story>
</Preview>

## Secondary Buttons

<Story name="Secondary">
  <SecondaryButton disabled={boolean("Disable Button", false)} onClick={action('button-click')}>Hello World</SecondaryButton>
</Story>

## Tertiary Buttons

<Story name="Tertiary">
  <TertiaryButton disabled={boolean("Disable Button", false)} onClick={action('button-click')}>Hello World</TertiaryButton>
</Story>
